name: Run-tests

on:
  push:

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Hello world!"
      - run: exit 1

  deploy:
    needs: [tests]
    runs-on: ubuntu-latest

    steps:
      - run: echo "Lets deploy!"
